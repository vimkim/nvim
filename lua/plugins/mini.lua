return {}

-- return {
--   {
--     "echasnovski/mini.trailspace",
--     version = false,
--     lazy = true, -- needed for snacks.nvim - 1
--     event = "BufReadPost", -- needed for snacks.nvim - 2
--     opts = {}, -- needed for startup, same as require("mini.trailspace").setup({})
--   },
-- }
